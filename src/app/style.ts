import { css } from "@/styled-system/css";
export { css } from "@/styled-system/css";

export const carouselDescription = css({
  display: 'flex',
  flexDirection: 'row',
  gap: 4,
})
export const descriptionAmount = css({
  color: 'success',
  marginLeft: '-.5rem',
})
export const mainWrapper = css({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  gap: 10,
  maxWidth: 'full',
  padding: 5,
  paddingBottom: 24,
  color: 'low-contrast',
  borderBottomWidth: '1px',
  borderTopWidth: '1px',
})

